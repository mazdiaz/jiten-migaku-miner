# Copy to .env.local. Never commit real values.
DATABASE_URL=postgresql://miner:miner@127.0.0.1:5432/miner
# Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))"
AUTH_SECRET=
AUTH_GITHUB_ID=
AUTH_GITHUB_SECRET=
# Immutable numeric account ID, not a username.
OWNER_GITHUB_ID=46370875
# Local development only. Set this to your exact HTTPS origin on Vercel.
AUTH_URL=http://127.0.0.1:8920
AUTH_TRUST_HOST=true
